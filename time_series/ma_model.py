# Moving Average model, similar to AR but uses past noise terms (simplified here)

def moving_average_model(data: list[float], theta: list[float], c: float = 0.0) -> list[float]:
    """
    MA(q) model prediction.
    theta = MA coefficients
    """
    # Placeholder - real MA model requires error terms
    pass
